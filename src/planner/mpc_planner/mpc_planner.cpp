#include "planner/mpc_planner/mpc_planner.h"

namespace planning {
namespace backend {

MPCPlanner::MPCPlanner(const params::MPCSolverParams& mpc_solver_params){
    // 1. set the MPC planner config
    // setParams(config);
    // 2. set the MPC planner solver
    // setSolver();
};
}
}   // namespace planning